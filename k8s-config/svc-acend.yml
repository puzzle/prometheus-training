apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-basics-training-acend
  name: prometheus-basics-training-acend
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: prometheus-basics-training-acend
  sessionAffinity: None
  type: ClusterIP
