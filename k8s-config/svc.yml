apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-basics-training
  name: prometheus-basics-training
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: prometheus-basics-training
  sessionAffinity: None
  type: ClusterIP
