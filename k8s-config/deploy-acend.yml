apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    keel.sh/policy: "force"
    keel.sh/match-tag: "true"
  labels:
    app: prometheus-basics-training-acend
  name: prometheus-basics-training-acend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-basics-training-acend
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: prometheus-basics-training-acend
    spec:
      containers:
      - image: puzzle/prometheus-basics-training:acend
        imagePullPolicy: Always
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 2
          successThreshold: 1
          timeoutSeconds: 2
        name: training
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 2
          successThreshold: 2
          timeoutSeconds: 2
        resources:
          requests:
            cpu: "20m"
            memory: "50M"
          limits:
            cpu: "200m"
            memory: "200M"
            
        securityContext:
          allowPrivilegeEscalation: false
          capabilities: {}
          privileged: false
          readOnlyRootFilesystem: false
          runAsNonRoot: false
        stdin: true
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        tty: true
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
